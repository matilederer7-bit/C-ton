# Render Free Tier Alignment Issues

## Non-Blocking

- Render free Postgres has a 30-day lifetime and a one-free-database-per-workspace limit. This is acceptable for demo / preview, but should not be mistaken for a stable long-term environment.

## External-Only

- Final confirmation still depends on opening the updated Blueprint in Render after the repo is reachable by Render, because pricing is ultimately shown by the Render dashboard for the target workspace.
