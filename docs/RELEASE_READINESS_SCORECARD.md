# Release readiness scorecard

Date: 2026-09-15. Branch: `claude/release-readiness-reintegration` from canonical master `0e53998bb4bb24b191a8d8fdfa921048c7b5a8b5` (PR #9 R9C financial rails + migrations 067/068, PR #12 deterministic confidentiality proof, PR #13 hardened UX all merged). Source material: `claude/release-readiness-night` 63a108f (ahead 8 / behind 24), ported file by file, never merged or rebased. Evidence machine: Windows 11, Node 24.13.1 (CI/Docker use Node 22), PostgreSQL 18 local, canonical `npm ci`, **no Docker engine locally**. Percentages describe how much of each area is PROVEN by an automated, repeatable gate for this commit; they are not inflated for hosted or provider work that was not executed.

Two numbers are reported and never combined:

- **TECHNICAL READINESS** — can this commit go to staging/production as code? Answered by `npm run release:preflight -- --profile full` (`TECHNICAL_READINESS`), the backend/web CI workflows and the browser proof.
- **REAL-MONEY ACTIVATION READINESS** — may real provider money flow? Answered ONLY by `config/real-money-release-policy.json` (`REAL_MONEY_ACTIVATION: BLOCKED`). A green technical preflight never changes it.

Regenerate the live numbers with `npm run release:preflight -- --profile full` (`.release-artifacts/release-preflight.md`), `npm run release:checklist` and `npm run release:owner-check`.

| Area | Status | % | Evidence (2026-09-15, master `0e53998` source) | Next step |
|---|---|---|---|---|
| Repository hygiene | COMPLETE | 95 | `repository-hygiene` PASS with the working-tree WARNING only while the port was uncommitted; no tracked artefacts/env files; whitespace clean (`git diff --check` on the range); 61 migrations LF in the index + `.gitattributes` pin; `.worktrees/` + `.release-artifacts/` ignored (git and Docker); canonical scan policy with 5 tests | keep |
| Build | COMPLETE | 95 | TypeScript clean; demo bundle + mobile bundle DETERMINISTIC across two clean builds (`reproducible-build` PASS); mobile/PWA contract PASS; Docker static readiness 6/6; **image build not executed here (no engine)** | image build proven by `release-readiness.yml` / `docker-release-lab` on the PR |
| Tests | COMPLETE (local) | 95 | payments 43/43, security 40/40 inside the full preflight; route-authorization behavioural gate PASS; release-tool tests 60/60 (incl. 9 money mutants, 5 legal mutants, route/secret/scan-policy negative controls, 2 activation-separation controls); browser proof 324/324 at 320/390/430/768/1280/1440; the ten groups + `test:all` run in `backend-gates` on the PR | GitHub CI on the PR (`backend-gates`, `web-runtime-depth`, `release-readiness`) |
| Security | PARTIAL | 85 | semantic raw-card scan PASS (318 files, 3 documented allow-list evidence identifiers); secret/PII scan 902 files 0 findings; logging hygiene 40 log calls, 0 NEVER_LOG, 1 SENSITIVE unmasked (`recipient_ref`, LOG-1); route inventory 213/213 classified (117 protected, 8 deliberate public-write); HTTP smoke 11/11 + GAP-HTTP-1 (`/readiness` no-store); legal gate PASS with the production KYC step as OWNER_DECISION; startup matrix 20 PASS + 6 documented runtime gaps (release gate is the only defence) | runtime hardening items listed in `docs/RUNTIME_ENVIRONMENT_POLICY.md`; owner KYC decision |
| Database | COMPLETE | 95 | migration preflight 10/10 on the CURRENT 61-migration set, high-water 068 (fresh install 61 + rerun 0, upgrade from `origin/master` drift 0, partial ledger, CRLF-era ledger accepted as EOL variants, tampered checksum refused, dirty ledger refused, failing migration atomic x2); backup/restore rehearsal 9/9 (75 tables, 997 columns, 73 FKs, 254 indexes, 19 triggers, 24 functions, ledger 61 rows identical, Hebrew intact, 8 % fee row, 0 commission columns, dump free of secret shapes); doctor + repair controls; LF-canonical checksums equal the staging ledger digests | `migrations:doctor` against the target database (owner console, read-only) before each hosted deploy |
| Financial safety | TECHNICALLY PROVEN, ACTIVATION BLOCKED | 70 | money/tax canon gate: executed fee vectors (100/0, 118/18, refund sign, VAT clamp) + AST invariants on master's R9C source, 8 % fee on gross incl. shipping, fee base excludes only buyer VAT, distributor commission 0; no-real-money proof 16/16 (runtime guard executed: live refused outside production, mock refused in production, sandbox host refused for live); every checked-in target on `mockpay`; `FINANCIAL_BRANCH_NOT_INTEGRATED` cleared with evidence | **F-13 provider contract unresolved**; Grow live verification; owner activation; adversarial review |
| UX | PRESERVED | 100 (of PR #13 scope) | zero `src/` or `web/` changes in this branch; browser proof 324/324 on the six established viewports; PR #12 confidentiality proof file untouched | hosted browser proofs per `docs/RC_STAGING_SMOKE.md` after deploy |
| Infrastructure | PARTIAL | 75 | runtime environment policy for 5 targets evaluated against `render.yaml` (web + worker), both compose files and `.env.demo.example`: 0 FAIL, `OTP_HASH_SALT` missing on Render = WARNING; supply chain on a canonical install: lockfiles clean, `npm ls` clean, 5 duplicate majors, 20 advisories (4 production high = owner upgrade decision); Node engines aligned (22 in Dockerfile/CI); **Docker lab SKIPPED_ENVIRONMENT locally; hosted staging NOT CHECKED** | Render: add `OTP_HASH_SALT`; first green `docker-release-lab`; dependency upgrade change |
| Observability | PARTIAL | 70 | health contract 6/6 incl. negative control (DB dropped -> `/health` 200, `/readiness` 503) and worker heartbeat; gaps HC-1..HC-4 documented; logging classification + gate | runtime changes for HC-1/HC-3 and LOG-1 (engineering, later) |
| Operations | COMPLETE | 90 | runbooks re-grounded on the merged architecture (GitHub code / Render runtime / Supabase Postgres+Auth / Grow disabled; Base44 never canonical): deployment (three separate actions), rollback, payment incident, database incident, security incident, data-access boundaries, parallel-agent model (port-when-far-behind rule); process guard + flake classifier + isolated-DB helper; owner one-command check with separated technical vs real-money answer | owner acknowledgement of the runbooks; first rollback rehearsal on staging (hosted, later) |
| Real-money activation | BLOCKED | 0 | `config/real-money-release-policy.json` BLOCKED with 4 uncleared reasons (F13_PROVIDER_CONTRACT_UNRESOLVED, GROW_LIVE_VERIFICATION_NOT_PERFORMED, PRODUCTION_PAYMENT_ACTIVATION_NOT_APPROVED, ADVERSARIAL_REVIEW_NOT_PERFORMED) and 1 cleared with evidence; `REAL_MONEY_ACTIVATION: BLOCKED` printed by preflight, proof, env gate, manifest, checklist, owner check | see `docs/REAL_MONEY_RELEASE_GOVERNANCE.md`: each reason needs dated evidence; runtime activation is a separate console action afterwards |

## Overall

- **TECHNICAL READINESS: ~90 %** — full preflight on master-source: every executable gate PASS or PASS-with-documented-warnings, one gate SKIPPED_ENVIRONMENT (Docker lab, to be proven by CI), zero FAIL; browser proof 324/324; payments/security groups green. The remaining 10 % is what only CI/hosted can prove: the Docker lab image build, the first green run of `release-readiness.yml`, and the staging deploy of this commit.
- **REAL-MONEY ACTIVATION READINESS: 0 %** — BLOCKED by governance (F-13 REAL_MONEY_BLOCKER, Grow live verification not performed, owner activation not approved, adversarial review not performed). Real money = 0. Grow not called.
- These two numbers are deliberately not combined into one figure.

## Open items by owner

| Owner | Item |
|---|---|
| Owner | KYC approval step in production intended? (`legal_compliance_gate` OWNER_DECISION) |
| Owner (hosted) | `OTP_HASH_SALT` on both Render services; Render Starter plan; Supabase Site URL |
| Owner + reviewer | F-13 provider-contract resolution (R9C rails are on master; the provider-side facts are not proven) |
| Provider (Grow) | sandbox `userId`/`pageCode`; live verification |
| Senior skeptical engineer | adversarial review of the integrated master (financial rails, UX, release tooling) |
| Engineering | runtime gaps (`docs/RUNTIME_ENVIRONMENT_POLICY.md`), HC-1/HC-3, LOG-1, GAP-HTTP-1..3, dependency upgrades (`docs/SUPPLY_CHAIN_STATUS.md`), optional removal of `vitest` |
| Engineering (observed while grounding the runbooks, `src/` untouched here) | `src/admin_mission_control.ts` outbox trace query selects `outbox_events.event_id` (the column is `event_uuid`) and returns empty through `safeQuery`; the `payment_attempts.provider_reference` half of the 2026-09-14 observation is resolved by migration 067; `service_role` retains EXECUTE on `siton_inventory_rpc`; tracking-token revoke helper has no route caller |
| CI | first green run of `release-readiness.yml` incl. `docker-release-lab` on this PR |
