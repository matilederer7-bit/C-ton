{
  "task": "Team-lead smoke: the outbox worker must not write personal data or credentials from error objects into hosted logs. Its pino logger currently serializes raw errors (message, stack and pg fields such as detail) to Render logs.",
  "lead": "claude-lead",
  "base": "origin/master",
  "open_branches": [
    "origin/chatgpt/render-rate-limit-hardening-20260920",
    "origin/codex/senior-engineering-review-20260921",
    "origin/chatgpt/phone-team-gpt6-openai-only-20260923",
    "origin/codex/bootstrap-reconcile-20260924"
  ],
  "interface_contract": {
    "src/log_redaction.ts": "export function errorLogSerializer(err: unknown): Record<string, unknown>. Start from pino.stdSerializers.err, then scrub every string value (recursively, including message, stack, detail, hint, where, cause and custom properties) with scrubText from src/error_monitoring.ts. Keep type, code and UUIDs. Bound total size. Never throw.",
    "src/worker.ts": "export the worker's pino instance as `logger`, created with serializers { err: errorLogSerializer }."
  },
  "assignments": [
    {
      "id": "B1",
      "agent": "claude-subagent",
      "role": "builder",
      "scope": "Implementation: error log serializer and worker logger wiring",
      "allowed": ["src/log_redaction.ts", "src/worker.ts"],
      "forbidden": ["src/app.ts", "src/error_monitoring.ts", "src/migrations/", "tests/", ".github/", "PROJECT_STATUS.md"],
      "depends_on": [],
      "dod": [
        "src/log_redaction.ts implements the interface contract",
        "src/worker.ts exports `logger` using the serializer; no behaviour change besides serialization",
        "npx tsc -p tsconfig.demo.json --noEmit passes",
        "worker test group passes on a disposable PostgreSQL"
      ]
    },
    {
      "id": "B2",
      "agent": "claude-subagent",
      "role": "builder",
      "scope": "Adversarial tests written from the interface contract, in parallel with B1",
      "allowed": ["tests/log_error_scrub_security_validation.ts"],
      "forbidden": ["src/", ".github/", "PROJECT_STATUS.md"],
      "depends_on": ["B1"],
      "dependency_note": "Authoring is parallel against the fixed interface contract; only the passing run depends on B1's integrated result.",
      "dod": [
        "hostile pg-style and nested errors carry email, phone, card, JWT, bearer, password and IP values; none survive serialization",
        "type, code and UUID correlation ids survive",
        "the live worker logger's err serializer is errorLogSerializer",
        "serializer never throws on hostile input (circular, getters that throw, huge strings)",
        "test fails when scrubbing is removed (mutation check by the lead)"
      ]
    },
    {
      "id": "R1",
      "agent": "claude-subagent",
      "role": "reviewer",
      "senior": true,
      "scope": "Independent senior security review of the integrated diff (security-auditor posture, read-only)",
      "reviews": ["B1", "B2"],
      "depends_on": ["B1", "B2"],
      "dod": ["verdict with concrete failure scenarios for every finding; no blocking finding open at merge"]
    },
    {
      "id": "R2",
      "agent": "codex",
      "role": "reviewer",
      "scope": "Independent cross-provider review through the ChatGPT Codex connector on the Pull Request",
      "reviews": ["B1", "B2"],
      "depends_on": ["B1", "B2"],
      "dod": ["every Codex finding is fixed or answered on its thread before merge"]
    }
  ]
}
