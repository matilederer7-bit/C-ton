{
  "description": "Deliberate dummy values the secret/PII scanner (scripts/secret_pii_scan.cjs) must ignore. Every entry names ONE file, the detector id and the exact matched text (or a prefix with `match_prefix`), plus a reason. Wildcards are refused; stale entries fail the scan.",
  "allow": [],
  "note": "Currently empty: compose/CI database URLs use local hosts and documented demo passwords, which the database-url-credential detector already excludes by rule (LOCAL_DB_HOSTS + PLACEHOLDER_PASSWORDS)."
}
