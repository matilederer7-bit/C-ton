# Legacy Render snapshot

These files are historical evidence only. They are not a production deployment target,
are not evaluated by the active CI gate, and must not be used to deploy Siton V1.

Canonical production ownership is declared in `base44/runtime-manifest.json` and
`docs/CANONICAL_ARCHITECTURE_V1.md`. Docker remains at the repository root only as a
portable local/test runtime; the former Render Blueprint and Procfile are quarantined here.
