/**
 * RETIRED destructive development reset.
 *
 * This path intentionally performs no database operation. Use the canonical
 * migration manifest and migrator; database destruction is never a migration.
 */
export {};
